const io=new IntersectionObserver(e=>e.forEach(x=>x.isIntersecting&&x.target.classList.add('visible')),{threshold:.12});document.querySelectorAll('section').forEach(x=>io.observe(x));document.querySelector('.hamb').onclick=()=>document.querySelector('nav').classList.toggle('open');document.querySelector('form').onsubmit=e=>{e.preventDefault();const b=e.currentTarget.querySelector('button');b.innerHTML='ЗАПРОС ПОЛУЧЕН ✓';b.disabled=true};