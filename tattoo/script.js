const transition = document.querySelector('.page-transition');
const links = document.querySelectorAll('a[href^="#"]');
window.addEventListener('load', () => { window.setTimeout(() => transition.classList.add('is-done'), 950); });
links.forEach(link => { link.addEventListener('click', event => { const target = document.querySelector(link.getAttribute('href')); if (!target) return; event.preventDefault(); transition.classList.remove('is-done'); transition.style.pointerEvents = 'auto'; window.setTimeout(() => { target.scrollIntoView({behavior:'smooth', block:'start'}); window.setTimeout(() => transition.classList.add('is-done'), 720); }, 500); }); });