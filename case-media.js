document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.case-image').forEach(box=>{const bg=getComputedStyle(box).backgroundImage;const matches=[...bg.matchAll(/url\(["']?([^"')]+)["']?\)/g)];const src=matches.at(-1)?.[1];if(!src)return;box.innerHTML='';box.style.backgroundImage='none';box.style.height='auto';box.style.aspectRatio='auto';const img=document.createElement('img');img.src=src;img.alt='Project preview';img.loading='lazy';box.appendChild(img)})});